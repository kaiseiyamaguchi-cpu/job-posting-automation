OLP様向けデモツール「求人オートメーション」の開発手順書（タタキ台）です。
最短工数で「ログイン済みのブラウザを乗っ取る」方式の実装を想定しています。

---

# OLP様向け 求人オートメーションデモ 開発手順書

## 1. システム構成・全体像

**「スプレッドシートをリモコンにして、手元のChromeを遠隔操作する」** 構成です。

*   **UI/DB:** Google Spreadsheet
*   **AI処理:** Google Apps Script (GAS) + **Gemini API** (gemini-1.5-flash)
*   **ブラウザ操作:** Local Python + Playwright
*   **連携方式:** Pythonがスプレッドシートを常時監視（ポーリング）
*   **認証方式:** **Application Default Credentials (ADC)** ※サービスアカウントキー不要

### 対応求人媒体

| 媒体 | 運営 | 特徴 |
|------|------|------|
| **Indeed** | Indeed Japan | 最大手の求人検索エンジン |
| **求人ボックス** | カカクコム | 無料掲載可能、Indeedに次ぐ利用者数 |
| **エンゲージ（engage）** | エン・ジャパン | 無料で採用サイト作成・求人掲載可能 |

## 2. 事前準備

### 2-1. GCPプロジェクト設定

1.  **GCPプロジェクト作成**
    - [GCPコンソール](https://console.cloud.google.com/) で新規プロジェクト作成
    - プロジェクト名: `olp-job-automation`

2.  **必要なAPIを有効化**
    - `Google Sheets API`
    - `Google Drive API`  
    - `Generative Language API` (Gemini)

### 2-2. gcloud CLI セットアップ（ADC認証）

> ⚠️ **組織ポリシーでサービスアカウントキーの発行が制限されている場合、この方式を使用します**

1.  **gcloud CLI インストール**
    - https://cloud.google.com/sdk/docs/install からダウンロード・インストール
    - 確認: `gcloud --version`

2.  **プロジェクト設定**
    ```bash
    gcloud config set project <プロジェクトID>
    ```

3.  **ADC（Application Default Credentials）認証**
    ```bash
    gcloud auth application-default login
    ```
    - ブラウザが開くのでGoogleアカウントでログイン
    - 認証情報が `~/.config/gcloud/application_default_credentials.json` に保存される
    - **この認証情報をPythonが自動で読み取ります（credentials.jsonの配置不要）**

### 2-3. Gemini APIキー取得（GAS用）

- [APIとサービス] > [認証情報] > [認証情報を作成] > [APIキー]
- このキーはGAS（Apps Script）で使用

### 2-4. ローカル環境

- **Python 3.9以上** がインストールされていること

---

## 3. スプレッドシート & GASの実装

### 3-1. シート作成
新規スプレッドシートを作成し、以下のヘッダーを設定します。

| 行/列 | A | B | C | D | E |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **1** | **元ネタ（Input）** | **Indeed用原稿** | **求人ボックス用原稿** | **エンゲージ用原稿** | **ステータス** |
| **2** | （ここにテスト入力） | （AI生成で埋まる） | （AI生成で埋まる） | （AI生成で埋まる） | 未着手 / 投稿開始 / 完了 |

※ シート名を `Sheet1` とします。
※ **ADC認証を使うため、サービスアカウントへの共有は不要です**（自分のGoogleアカウントで操作）

### 3-2. GAS実装（AI生成）

#### ステップ1: スクリプトプロパティにAPIキー設定
1. 「拡張機能」>「Apps Script」を開く
2. 左側メニュー「プロジェクトの設定」（歯車アイコン）をクリック
3. 「スクリプトプロパティ」セクションで「スクリプトプロパティを追加」
4. プロパティ名: `GEMINI_API_KEY`、値: 取得したAPIキー

#### ステップ2: コード実装
以下のコードを `コード.gs` に記述します。

```javascript
/**
 * スプレッドシート起動時にカスタムメニューを追加
 */
function onOpen() {
  const ui = SpreadsheetApp.getUi();
  ui.createMenu('OLPツール')
    .addItem('AI原稿生成', 'generateJobPosts')
    .addToUi();
}

/**
 * Gemini APIを呼び出す関数
 * @param {string} prompt - 送信するプロンプト
 * @returns {string} - Geminiからの応答テキスト
 */
function callGeminiAPI(prompt) {
  const apiKey = PropertiesService.getScriptProperties().getProperty('GEMINI_API_KEY');
  const url = `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`;
  
  const payload = {
    contents: [
      {
        parts: [
          { text: prompt }
        ]
      }
    ],
    generationConfig: {
      temperature: 0.7,
      maxOutputTokens: 4096
    }
  };
  
  const options = {
    method: 'post',
    contentType: 'application/json',
    payload: JSON.stringify(payload),
    muteHttpExceptions: true
  };
  
  try {
    const response = UrlFetchApp.fetch(url, options);
    const json = JSON.parse(response.getContentText());
    
    if (json.error) {
      throw new Error(json.error.message);
    }
    
    return json.candidates[0].content.parts[0].text;
  } catch (e) {
    throw new Error('Gemini API呼び出しエラー: ' + e.message);
  }
}

/**
 * メイン処理: A列の入力から3媒体用の原稿を生成
 */
function generateJobPosts() {
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName('Sheet1');
  const lastRow = sheet.getLastRow();
  
  // A列の最新の入力を取得
  const inputText = sheet.getRange(lastRow, 1).getValue();
  if (!inputText) {
    SpreadsheetApp.getUi().alert('A列に求人情報を入力してください。');
    return;
  }

  // プロンプト組み立て
  const prompt = `
あなたは求人原稿のプロライターです。
以下の求人情報を元に、3つの求人媒体向けの原稿を作成してください。

【出力形式】
必ず以下のフォーマットで出力してください。余計な説明は不要です。
---INDEED---
(Indeed向けの原稿をここに)
---KYUJINBOX---
(求人ボックス向けの原稿をここに)
---ENGAGE---
(エンゲージ向けの原稿をここに)

【Indeed向け原稿の要件】
- 検索でヒットしやすいキーワードを含める
- 堅めでビジネスライクな文体
- 具体的な条件（給与、勤務地、勤務時間等）を明記
- 箇条書きを活用して読みやすく

【求人ボックス向け原稿の要件】
- Indeed同様に検索キーワードを意識
- 職種名を明確に
- 「急募」「未経験歓迎」などの訴求ワードを適度に使用
- 応募条件を分かりやすく記載

【エンゲージ向け原稿の要件】
- 会社の魅力・雰囲気を伝える文章
- 働く人の声や職場環境の説明を含める
- 採用サイトらしい丁寧で親しみやすい文体
- 成長機会やキャリアパスにも言及

【求人情報】
${inputText}
`;

  try {
    // ローディング表示
    SpreadsheetApp.getActiveSpreadsheet().toast('Gemini APIで原稿生成中...', '処理中', -1);
    
    // Gemini API呼び出し
    const response = callGeminiAPI(prompt);
    
    // レスポンスをパース
    const indeedMatch = response.match(/---INDEED---\n?([\s\S]*?)(?=---KYUJINBOX---|$)/);
    const kyujinboxMatch = response.match(/---KYUJINBOX---\n?([\s\S]*?)(?=---ENGAGE---|$)/);
    const engageMatch = response.match(/---ENGAGE---\n?([\s\S]*?)$/);
    
    const indeedText = indeedMatch ? indeedMatch[1].trim() : '';
    const kyujinboxText = kyujinboxMatch ? kyujinboxMatch[1].trim() : '';
    const engageText = engageMatch ? engageMatch[1].trim() : '';
    
    // 結果をセルに書き込む
    sheet.getRange(lastRow, 2).setValue(indeedText);      // B列: Indeed
    sheet.getRange(lastRow, 3).setValue(kyujinboxText);   // C列: 求人ボックス
    sheet.getRange(lastRow, 4).setValue(engageText);      // D列: エンゲージ
    sheet.getRange(lastRow, 5).setValue('AI生成済');       // E列: ステータス
    
    SpreadsheetApp.getActiveSpreadsheet().toast('原稿生成が完了しました！', '完了', 3);
    
  } catch (e) {
    SpreadsheetApp.getUi().alert('エラーが発生しました:\n' + e.message);
  }
}
```

---

## 4. ローカルPython環境の構築

### 4-1. フォルダ構成
作業フォルダを作成し、以下を配置します。

```text
/olp-job-automation
  ├─ main.py            # メインスクリプト
  ├─ config.py          # 設定ファイル
  ├─ spreadsheet.py     # スプレッドシート操作
  ├─ browser.py         # ブラウザ操作
  ├─ requirements.txt   # 依存ライブラリ
  └─ .gitignore         # Git除外設定
```

> ⚠️ **credentials.json は不要です**（ADC認証を使用するため）

### 4-2. requirements.txt
```text
gspread
google-auth
playwright
```

### 4-3. .gitignore
```text
venv/
__pycache__/
*.pyc
.env
```

### 4-4. セットアップコマンド
```bash
# フォルダ作成 & 移動
mkdir olp-job-automation && cd olp-job-automation

# 仮想環境作成 & アクティベート
python3 -m venv venv
source venv/bin/activate  # Mac/Linux
# venv\Scripts\activate   # Windows

# ライブラリインストール
pip install -r requirements.txt
playwright install chromium
```

---

## 5. Pythonコード実装

### 5-1. config.py
```python
"""設定ファイル"""

# スプレッドシート設定
SHEET_NAME = "OLP求人自動投稿デモ"  # スプレッドシート名
WORKSHEET_NAME = "Sheet1"           # シート名

# Chrome設定
CHROME_DEBUG_URL = "http://localhost:9222"

# 監視設定
POLLING_INTERVAL = 3  # 秒

# 投稿先設定 ('indeed' / 'kyujinbox' / 'engage' / 'google_form')
TARGET_SITE = 'indeed'

# 予備: Googleフォーム設定（各媒体接続失敗時用）
GOOGLE_FORM_URL = ""  # デモ用フォームのURL

# 各媒体のURL（参考）
MEDIA_URLS = {
    'indeed': 'https://jp.indeed.com/hire',
    'kyujinbox': 'https://xn--pckua2a7gp15o89zb.com/%E6%B1%82%E4%BA%BA%E6%8E%B2%E8%BC%89/',
    'engage': 'https://en-gage.net/company/',
}
```

### 5-2. spreadsheet.py（ADC認証版）
```python
"""スプレッドシート操作モジュール（ADC認証版）"""
import gspread
from google.auth import default
from google.auth.transport.requests import Request

from config import SHEET_NAME, WORKSHEET_NAME

SCOPES = [
    'https://www.googleapis.com/auth/spreadsheets',
    'https://www.googleapis.com/auth/drive.readonly'
]


def connect_sheet():
    """
    ADC（Application Default Credentials）を使ってスプレッドシートに接続
    
    事前に `gcloud auth application-default login` を実行しておくこと
    """
    # ADC認証情報を自動取得（~/.config/gcloud/application_default_credentials.json）
    credentials, project = default(scopes=SCOPES)
    
    # 認証情報が期限切れの場合はリフレッシュ
    if credentials.expired:
        credentials.refresh(Request())
    
    # gspread クライアント作成
    client = gspread.authorize(credentials)
    
    # スプレッドシートを開く
    sheet = client.open(SHEET_NAME).worksheet(WORKSHEET_NAME)
    return sheet


def get_pending_rows(sheet):
    """ステータスが「投稿開始」の行を取得"""
    records = sheet.get_all_records()
    pending = []
    for i, row in enumerate(records):
        if row.get('ステータス') == '投稿開始':
            pending.append({
                'row_num': i + 2,  # ヘッダー行 + 0-indexed
                'input': row.get('元ネタ（Input）'),
                'indeed_text': row.get('Indeed用原稿'),
                'kyujinbox_text': row.get('求人ボックス用原稿'),
                'engage_text': row.get('エンゲージ用原稿'),
            })
    return pending


def update_status(sheet, row_num, status):
    """指定行のステータスを更新"""
    sheet.update_cell(row_num, 5, status)  # E列 = 5
```

### 5-3. browser.py
```python
"""ブラウザ操作モジュール"""
from playwright.sync_api import sync_playwright

from config import CHROME_DEBUG_URL, TARGET_SITE, GOOGLE_FORM_URL


def connect_to_chrome():
    """デバッグモードのChromeに接続"""
    p = sync_playwright().start()
    browser = p.chromium.connect_over_cdp(CHROME_DEBUG_URL)
    return p, browser


def move_mouse_for_demo(page):
    """デモ演出用: マウスをゆっくり動かす"""
    page.mouse.move(100, 100)
    page.mouse.move(300, 200, steps=20)
    page.mouse.move(500, 300, steps=20)


def post_to_indeed(page, text):
    """
    Indeed求人投稿フォームにテキストを入力
    ※セレクタはデモ前に要確認・調整
    """
    # 例: 求人説明欄への入力
    # page.fill('textarea#jobDescription', text)
    # page.fill('input#jobTitle', 'タイトル')
    
    # デモ用: まずはマウス移動だけ
    move_mouse_for_demo(page)
    print(f"✅ Indeed入力完了（セレクタ要調整）")


def post_to_kyujinbox(page, text):
    """
    求人ボックスの求人投稿フォームにテキストを入力
    ※セレクタはデモ前に要確認・調整
    """
    # 例: 求人説明欄への入力
    # page.fill('textarea.job-description', text)
    
    # デモ用: まずはマウス移動だけ
    move_mouse_for_demo(page)
    print(f"✅ 求人ボックス入力完了（セレクタ要調整）")


def post_to_engage(page, text):
    """
    エンゲージの求人投稿フォームにテキストを入力
    ※セレクタはデモ前に要確認・調整
    """
    # 例: 求人説明欄への入力
    # page.fill('textarea[name="description"]', text)
    
    # デモ用: まずはマウス移動だけ
    move_mouse_for_demo(page)
    print(f"✅ エンゲージ入力完了（セレクタ要調整）")


def post_to_google_form(page, text):
    """予備: Googleフォームへの入力"""
    page.goto(GOOGLE_FORM_URL)
    # page.fill('textarea', text)
    move_mouse_for_demo(page)
    print(f"✅ Googleフォーム入力完了")


def run_automation(indeed_text, kyujinbox_text, engage_text):
    """自動投稿を実行"""
    print("🚀 Playwright起動: ブラウザへの接続を開始します...")
    
    try:
        p, browser = connect_to_chrome()
        context = browser.contexts[0]
        page = context.pages[0]
        
        print(f"📄 操作対象ページ: {page.title()}")
        
        if TARGET_SITE == 'indeed':
            post_to_indeed(page, indeed_text)
        elif TARGET_SITE == 'kyujinbox':
            post_to_kyujinbox(page, kyujinbox_text)
        elif TARGET_SITE == 'engage':
            post_to_engage(page, engage_text)
        else:
            post_to_google_form(page, indeed_text)
        
        # 接続を切断（ブラウザは閉じない）
        browser.close()
        p.stop()
        return True
        
    except Exception as e:
        print(f"❌ エラー発生: {e}")
        print("ヒント: Chromeがデバッグモード(--remote-debugging-port=9222)で起動していますか？")
        return False
```

### 5-4. main.py
```python
"""メインスクリプト: スプレッドシート監視 & 自動投稿"""
import time

from config import POLLING_INTERVAL
from spreadsheet import connect_sheet, get_pending_rows, update_status
from browser import run_automation


def main():
    print("=" * 50)
    print("🚀 OLP求人オートメーション デモツール")
    print("=" * 50)
    print("📋 対応媒体: Indeed / 求人ボックス / エンゲージ")
    print("👀 スプレッドシート監視開始...")
    print(f"📊 ポーリング間隔: {POLLING_INTERVAL}秒")
    print("-" * 50)
    
    sheet = connect_sheet()
    print("✅ スプレッドシートに接続しました（ADC認証）")
    
    while True:
        try:
            # 「投稿開始」の行を取得
            pending_rows = get_pending_rows(sheet)
            
            for row in pending_rows:
                print(f"\n👉 {row['row_num']}行目の投稿リクエストを検知！")
                
                # 自動化実行
                success = run_automation(
                    row['indeed_text'],
                    row['kyujinbox_text'],
                    row['engage_text']
                )
                
                if success:
                    update_status(sheet, row['row_num'], '完了')
                    print("🎉 ステータスを『完了』に更新しました")
                else:
                    update_status(sheet, row['row_num'], 'エラー')
                    print("⚠️ ステータスを『エラー』に更新しました")
            
            time.sleep(POLLING_INTERVAL)
            
        except KeyboardInterrupt:
            print("\n\n👋 監視を終了します")
            break
        except Exception as e:
            print(f"⚠️ ループエラー: {e}")
            print("5秒後にリトライします...")
            time.sleep(5)


if __name__ == "__main__":
    main()
```

---

## 6. デモ実行手順（本番）

### Step 0: ADC認証の確認（初回のみ）
```bash
# まだ実行していない場合
gcloud auth application-default login
```

### Step 1: Chromeをデバッグモードで起動
すべてのChromeウィンドウを閉じた状態で、以下のコマンドを実行してChromeを立ち上げます。

**Mac (Terminal):**
```bash
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --remote-debugging-port=9222 --user-data-dir="/tmp/chrome_debug"
```

**Windows (PowerShell):**
```powershell
& "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222 --user-data-dir="C:\chrome_debug"
```

### Step 2: ログイン & ページ待機
1.  立ち上がったChromeで、Indeed（またはデモ用フォーム）にログインします。
2.  **「求人投稿フォーム」の画面を開いた状態**にしておきます。

### Step 3: Pythonスクリプト起動
ターミナルで監視プログラムを走らせます。
```bash
cd olp-job-automation
source venv/bin/activate
python main.py
```

### Step 4: デモ実演
1.  スプレッドシートを開く。
2.  A列に適当なメモを入力する。
3.  メニュー「OLPツール」>「AI原稿生成」をクリック → 3媒体分の文章が生成される。
4.  ステータスを「投稿開始」に変更する。
5.  **（Chromeが勝手に動き出す）** → 成功！

---

## 7. 注意点・リスクヘッジ

*   **セレクタ（Selector）の確認:**
    各求人媒体のHTML構造は頻繁に変わります。`page.fill('#id', text)` の `#id` 部分が正しいか、必ずデモ直前にブラウザの「検証ツール」で確認してください。
*   **予備プラン:**
    もし各媒体のセキュリティが強すぎて入力できない場合に備え、Googleフォーム等で作った「ダミー求人入力画面」も用意し、`config.py` の `TARGET_SITE` で切り替えられるようにしています。
*   **ADC認証の有効期限:**
    - ADC認証は通常数時間〜数日で期限切れになります
    - 期限切れの場合は再度 `gcloud auth application-default login` を実行してください
    - コード内で自動リフレッシュ処理を入れています

---

## 8. トラブルシューティング

### Q: `gcloud auth application-default login` でエラーが出る
**A:** gcloud CLIが正しくインストールされているか確認してください。
```bash
gcloud --version
```

### Q: Pythonでスプレッドシートに接続できない
**A:** 以下を確認してください。
1. `gcloud auth application-default login` を実行済みか
2. スプレッドシート名（`SHEET_NAME`）が正確か
3. Google Sheets API / Drive API が有効化されているか

### Q: 認証情報が見つからないエラー
**A:** ADC認証ファイルの場所を確認：
```bash
ls ~/.config/gcloud/application_default_credentials.json
```
ファイルが存在しない場合は `gcloud auth application-default login` を再実行してください。
