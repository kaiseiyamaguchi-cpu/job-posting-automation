export default function AdminThreadsPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-3xl font-bold text-white">スレッド監視（管理者）</h1>
      <p className="text-slate-400">
        チャット監視は提案デモでは必須ではないため簡易ページにしています。必要ならスレッド一覧と最新メッセージを表示できます。
      </p>
    </div>
  );
}

