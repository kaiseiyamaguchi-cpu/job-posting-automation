# 技術仕様書

## 🏗️ アーキテクチャ概要

```
┌─────────────────────────────────────────────────────┐
│                   ユーザー                           │
└──────────────────┬──────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────┐
│              Vercel (Frontend)                      │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │          Next.js 15 (App Router)             │  │
│  │                                              │  │
│  │  ┌────────────┐  ┌────────────┐            │  │
│  │  │   Pages    │  │    API     │            │  │
│  │  │  (React)   │  │  Routes    │            │  │
│  │  └────────────┘  └────────────┘            │  │
│  └──────────────────────────────────────────────┘  │
└──────────────────┬──────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────┐
│            Supabase (Backend)                       │
│                                                     │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  │
│  │    Auth    │  │  Database  │  │  Realtime  │  │
│  │            │  │(PostgreSQL)│  │(WebSocket) │  │
│  └────────────┘  └────────────┘  └────────────┘  │
└─────────────────────────────────────────────────────┘
```

---

## 🛠️ 技術スタック

### Frontend

| 技術 | バージョン | 用途 |
|------|-----------|------|
| **Next.js** | 15.x | Reactフレームワーク |
| **TypeScript** | 5.x | 型安全性 |
| **Tailwind CSS** | 3.x | スタイリング |
| **shadcn/ui** | latest | UIコンポーネント |
| **React Hook Form** | 7.x | フォーム管理 |
| **Zod** | 3.x | バリデーション |

### Backend

| 技術 | バージョン | 用途 |
|------|-----------|------|
| **Supabase** | latest | BaaS（Backend as a Service） |
| **PostgreSQL** | 15.x | データベース |
| **Supabase Auth** | - | 認証・認可 |
| **Supabase Realtime** | - | リアルタイム通信 |

### Deployment

| 技術 | 用途 |
|------|------|
| **Vercel** | フロントエンド・APIホスティング |
| **Supabase Cloud** | バックエンドホスティング |

### Development Tools

| ツール | 用途 |
|--------|------|
| **ESLint** | コード品質チェック |
| **Prettier** | コードフォーマット |
| **Git** | バージョン管理 |
| **VS Code** | IDE |

---

## 📁 プロジェクト構造（詳細）

```
sales-agency-matching/
├── src/
│   ├── app/
│   │   ├── (auth)/                    # 認証前画面グループ
│   │   │   ├── layout.tsx
│   │   │   ├── login/page.tsx
│   │   │   └── register/page.tsx
│   │   │
│   │   ├── (dashboard)/               # 認証後画面グループ（企業・営業代行）
│   │   │   ├── layout.tsx
│   │   │   ├── dashboard/page.tsx
│   │   │   ├── agencies/
│   │   │   │   ├── page.tsx          # 営業代行一覧
│   │   │   │   └── [id]/page.tsx     # 営業代行詳細
│   │   │   ├── matching-requests/page.tsx
│   │   │   ├── messages/page.tsx
│   │   │   └── profile/page.tsx
│   │   │
│   │   ├── (admin)/                   # 管理者画面グループ
│   │   │   ├── layout.tsx
│   │   │   └── admin/
│   │   │       ├── dashboard/page.tsx
│   │   │       ├── users/page.tsx
│   │   │       ├── matching-requests/page.tsx
│   │   │       └── threads/page.tsx
│   │   │
│   │   ├── api/                       # API Routes
│   │   │   ├── auth/
│   │   │   │   ├── register/route.ts
│   │   │   │   ├── login/route.ts
│   │   │   │   ├── logout/route.ts
│   │   │   │   └── session/route.ts
│   │   │   ├── sales-agencies/
│   │   │   │   ├── route.ts          # GET（一覧）
│   │   │   │   └── [id]/route.ts     # GET（詳細）
│   │   │   ├── matching-requests/
│   │   │   │   ├── route.ts          # POST, GET
│   │   │   │   ├── received/route.ts # GET
│   │   │   │   └── [id]/route.ts     # PUT
│   │   │   ├── message-threads/
│   │   │   │   └── route.ts          # GET
│   │   │   ├── messages/
│   │   │   │   ├── route.ts          # GET, POST
│   │   │   │   └── mark-read/route.ts # PUT
│   │   │   ├── dashboard/
│   │   │   │   └── stats/route.ts
│   │   │   ├── profile/
│   │   │   │   ├── company/route.ts
│   │   │   │   └── agency/route.ts
│   │   │   └── admin/
│   │   │       ├── stats/route.ts
│   │   │       ├── users/route.ts
│   │   │       ├── matching-requests/route.ts
│   │   │       └── threads/route.ts
│   │   │
│   │   ├── layout.tsx                 # ルートレイアウト
│   │   ├── page.tsx                   # トップページ（リダイレクト）
│   │   └── globals.css
│   │
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── DashboardLayout.tsx
│   │   ├── auth/
│   │   │   ├── LoginForm.tsx
│   │   │   └── RegisterForm.tsx
│   │   ├── agency/
│   │   │   ├── AgencyCard.tsx
│   │   │   ├── AgencyDetail.tsx
│   │   │   └── SearchBar.tsx
│   │   ├── matching/
│   │   │   ├── RequestCard.tsx
│   │   │   ├── RequestModal.tsx
│   │   │   └── ApprovalButtons.tsx
│   │   ├── message/
│   │   │   ├── ThreadList.tsx
│   │   │   ├── ChatWindow.tsx
│   │   │   ├── MessageBubble.tsx
│   │   │   └── MessageInput.tsx
│   │   ├── dashboard/
│   │   │   ├── StatsCard.tsx
│   │   │   ├── RecentActivity.tsx
│   │   │   └── QuickActions.tsx
│   │   ├── profile/
│   │   │   ├── CompanyProfile.tsx
│   │   │   └── AgencyProfile.tsx
│   │   ├── admin/
│   │   │   ├── StatsOverview.tsx
│   │   │   └── UserTable.tsx
│   │   └── ui/                        # shadcn/ui components
│   │       ├── button.tsx
│   │       ├── input.tsx
│   │       ├── select.tsx
│   │       ├── textarea.tsx
│   │       ├── dialog.tsx
│   │       ├── badge.tsx
│   │       ├── card.tsx
│   │       └── table.tsx
│   │
│   ├── hooks/
│   │   ├── useAuth.ts                 # 認証状態管理
│   │   ├── useUser.ts                 # ユーザー情報取得
│   │   └── useRealtimeMessages.ts     # Realtime購読
│   │
│   ├── lib/
│   │   ├── supabase/
│   │   │   ├── client.ts              # クライアント側Supabase
│   │   │   └── server.ts              # サーバー側Supabase
│   │   ├── utils.ts                   # 汎用関数
│   │   └── constants.ts               # 定数定義
│   │
│   └── types/
│       ├── database.ts                # DB型定義
│       ├── api.ts                     # API型定義
│       └── index.ts                   # 共通型定義
│
├── supabase/
│   └── migrations/
│       ├── 001_initial_schema.sql
│       ├── 002_matching_schema.sql
│       └── 003_message_schema.sql
│
├── public/
│   ├── logo.svg
│   └── favicon.ico
│
├── .env.local                         # 環境変数（Git管理外）
├── .gitignore
├── package.json
├── package-lock.json
├── tsconfig.json
├── tailwind.config.ts
├── next.config.ts
├── postcss.config.mjs
├── components.json                    # shadcn/ui設定
└── README.md
```

---

## 🔐 環境変数

### `.env.local`

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### 本番環境（Vercel）

Vercelの環境変数設定で同じ変数を設定。

---

## 🗄️ Supabase設定

### 1. プロジェクト作成

1. https://supabase.com にアクセス
2. 「New Project」をクリック
3. プロジェクト名: `sales-agency-matching`
4. Database Password: 強力なパスワード設定
5. Region: `Tokyo (Northeast Asia)`
6. 「Create new project」をクリック

### 2. API Keys取得

Project Settings → API から以下を取得：
- `Project URL` → `NEXT_PUBLIC_SUPABASE_URL`
- `anon public` → `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### 3. Authentication設定

Authentication → Providers:
- Email: ✅ 有効化
- Confirm email: ❌ 無効化（MVP版）

### 4. Database設定

SQL Editor で各マイグレーションファイルを実行：
1. `001_initial_schema.sql`
2. `002_matching_schema.sql`
3. `003_message_schema.sql`

### 5. Realtime設定

Database → Replication:
- `messages` テーブルを有効化
- `message_threads` テーブルを有効化

---

## 📡 API実装パターン

### Next.js App Router API例

```typescript
// src/app/api/sales-agencies/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { createRouteHandlerClient } from '@supabase/auth-helpers-nextjs';
import { cookies } from 'next/headers';

export async function GET(request: NextRequest) {
  try {
    const supabase = createRouteHandlerClient({ cookies });
    
    // クエリパラメータ取得
    const searchParams = request.nextUrl.searchParams;
    const keyword = searchParams.get('keyword') || '';
    const industry = searchParams.get('industry') || '';
    const area = searchParams.get('area') || '';
    const page = parseInt(searchParams.get('page') || '1');
    const limit = parseInt(searchParams.get('limit') || '20');
    
    // クエリ構築
    let query = supabase
      .from('sales_agencies')
      .select('*', { count: 'exact' });
    
    // フィルター適用
    if (keyword) {
      query = query.or(`name.ilike.%${keyword}%,description.ilike.%${keyword}%`);
    }
    if (industry) {
      query = query.contains('specialties', [industry]);
    }
    if (area) {
      query = query.contains('areas', [area]);
    }
    
    // ページネーション
    const from = (page - 1) * limit;
    const to = from + limit - 1;
    query = query.range(from, to);
    
    // 実行
    const { data, error, count } = await query;
    
    if (error) throw error;
    
    return NextResponse.json({
      agencies: data,
      total: count,
      page,
      total_pages: Math.ceil((count || 0) / limit)
    });
    
  } catch (error) {
    console.error('Error:', error);
    return NextResponse.json(
      { error: 'サーバーエラーが発生しました' },
      { status: 500 }
    );
  }
}
```

---

## 🔄 Realtime実装パターン

### メッセージのリアルタイム購読

```typescript
// src/hooks/useRealtimeMessages.ts
import { useEffect, useState } from 'react';
import { createClientComponentClient } from '@supabase/auth-helpers-nextjs';
import type { Message } from '@/types/database';

export function useRealtimeMessages(threadId: string) {
  const [messages, setMessages] = useState<Message[]>([]);
  const supabase = createClientComponentClient();

  useEffect(() => {
    // 初期メッセージ取得
    const fetchMessages = async () => {
      const { data } = await supabase
        .from('messages')
        .select('*')
        .eq('thread_id', threadId)
        .order('created_at', { ascending: true });
      
      if (data) setMessages(data);
    };
    
    fetchMessages();

    // Realtime購読
    const channel = supabase
      .channel(`thread:${threadId}`)
      .on(
        'postgres_changes',
        {
          event: 'INSERT',
          schema: 'public',
          table: 'messages',
          filter: `thread_id=eq.${threadId}`
        },
        (payload) => {
          setMessages((prev) => [...prev, payload.new as Message]);
        }
      )
      .subscribe();

    return () => {
      supabase.removeChannel(channel);
    };
  }, [threadId, supabase]);

  return messages;
}
```

---

## 🔐 認証実装パターン

### Server Component での認証確認

```typescript
// src/app/(dashboard)/dashboard/page.tsx
import { createServerComponentClient } from '@supabase/auth-helpers-nextjs';
import { cookies } from 'next/headers';
import { redirect } from 'next/navigation';

export default async function DashboardPage() {
  const supabase = createServerComponentClient({ cookies });
  
  // 認証チェック
  const { data: { session } } = await supabase.auth.getSession();
  
  if (!session) {
    redirect('/login');
  }
  
  // ユーザー情報取得
  const { data: profile } = await supabase
    .from('profiles')
    .select('*')
    .eq('id', session.user.id)
    .single();
  
  // 統計データ取得
  // ...
  
  return (
    <div>
      {/* ダッシュボードUI */}
    </div>
  );
}
```

### Client Component での認証確認

```typescript
// src/hooks/useAuth.ts
import { useEffect, useState } from 'react';
import { createClientComponentClient } from '@supabase/auth-helpers-nextjs';
import { useRouter } from 'next/navigation';
import type { User } from '@supabase/supabase-js';

export function useAuth() {
  const [user, setUser] = useState<User | null>(null);
  const [loading, setLoading] = useState(true);
  const supabase = createClientComponentClient();
  const router = useRouter();

  useEffect(() => {
    const getUser = async () => {
      const { data: { session } } = await supabase.auth.getSession();
      setUser(session?.user ?? null);
      setLoading(false);
      
      if (!session) {
        router.push('/login');
      }
    };

    getUser();

    // 認証状態の変更を監視
    const { data: { subscription } } = supabase.auth.onAuthStateChange(
      (_event, session) => {
        setUser(session?.user ?? null);
      }
    );

    return () => {
      subscription.unsubscribe();
    };
  }, [supabase, router]);

  return { user, loading };
}
```

---

## 🎨 スタイリング規約

### Tailwind CSS設定

```typescript
// tailwind.config.ts
import type { Config } from 'tailwindcss';

const config: Config = {
  darkMode: ['class'],
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        border: 'hsl(var(--border))',
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))',
        },
        // ... shadcn/ui デフォルト設定
      },
    },
  },
  plugins: [require('tailwindcss-animate')],
};

export default config;
```

### カラー定義

```css
/* src/app/globals.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 222.2 84% 4.9%;       /* ダーク背景 */
    --foreground: 210 40% 98%;
    --primary: 217.2 91.2% 59.8%;       /* ブルー */
    --primary-foreground: 222.2 47.4% 11.2%;
    --secondary: 217.2 32.6% 17.5%;
    --muted: 217.2 32.6% 17.5%;
    --accent: 217.2 32.6% 17.5%;
    --destructive: 0 62.8% 30.6%;       /* レッド */
    --border: 217.2 32.6% 17.5%;
    --input: 217.2 32.6% 17.5%;
    --ring: 212.7 26.8% 83.9%;
    --radius: 0.5rem;
  }
}
```

---

## 📊 状態管理

### クライアント側の状態管理

```typescript
// ReactのuseStateとuseContextを使用
// グローバル状態が必要な場合のみContext API使用

// src/contexts/AuthContext.tsx
'use client';

import { createContext, useContext, useEffect, useState } from 'react';
import { User } from '@supabase/supabase-js';
import { createClientComponentClient } from '@supabase/auth-helpers-nextjs';

type AuthContextType = {
  user: User | null;
  loading: boolean;
};

const AuthContext = createContext<AuthContextType>({
  user: null,
  loading: true,
});

export function AuthProvider({ children }: { children: React.ReactNode }) {
  const [user, setUser] = useState<User | null>(null);
  const [loading, setLoading] = useState(true);
  const supabase = createClientComponentClient();

  useEffect(() => {
    const getSession = async () => {
      const { data: { session } } = await supabase.auth.getSession();
      setUser(session?.user ?? null);
      setLoading(false);
    };

    getSession();

    const { data: { subscription } } = supabase.auth.onAuthStateChange(
      (_event, session) => {
        setUser(session?.user ?? null);
      }
    );

    return () => {
      subscription.unsubscribe();
    };
  }, [supabase]);

  return (
    <AuthContext.Provider value={{ user, loading }}>
      {children}
    </AuthContext.Provider>
  );
}

export const useAuth = () => useContext(AuthContext);
```

---

## 🧪 テスト戦略

### 単体テスト
- コンポーネント単位のテスト（Jest + React Testing Library）
- ユーティリティ関数のテスト

### 統合テスト
- APIエンドポイントのテスト
- 認証フローのテスト

### E2Eテスト
- 主要ユーザーフローのテスト（Playwright）
- 企業登録→検索→申請→承認→メッセージ

**MVP版では手動テストのみ実施**

---

## 🚀 デプロイ手順

### Vercelデプロイ

1. **Vercelプロジェクト作成**
   ```bash
   vercel
   ```

2. **環境変数設定**
   - Vercel Dashboard → Settings → Environment Variables
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`

3. **デプロイ実行**
   ```bash
   vercel --prod
   ```

4. **カスタムドメイン設定（オプション）**
   - Vercel Dashboard → Domains
   - ドメイン追加・DNS設定

### Supabase本番環境

- 開発環境と本番環境を分ける場合、別プロジェクト作成
- マイグレーションファイルを本番環境で実行

---

## 📈 パフォーマンス最適化

### 画像最適化
```typescript
// Next.js Image コンポーネント使用
import Image from 'next/image';

<Image
  src="/logo.png"
  alt="Logo"
  width={200}
  height={50}
  priority
/>
```

### コード分割
```typescript
// 動的インポート
import dynamic from 'next/dynamic';

const ChatWindow = dynamic(() => import('@/components/message/ChatWindow'), {
  loading: () => <p>読み込み中...</p>,
  ssr: false,
});
```

### データフェッチング最適化
```typescript
// Server Componentでデータ取得（SSR）
// Client Componentでリアルタイム更新（CSR + Realtime）
```

---

## 🐛 デバッグ・ロギング

### 開発環境

```typescript
// src/lib/logger.ts
export const logger = {
  info: (message: string, data?: any) => {
    if (process.env.NODE_ENV === 'development') {
      console.log(`[INFO] ${message}`, data);
    }
  },
  error: (message: string, error?: any) => {
    console.error(`[ERROR] ${message}`, error);
  },
};
```

### Supabase クエリデバッグ

```typescript
const { data, error } = await supabase
  .from('messages')
  .select('*')
  .eq('thread_id', threadId);

if (error) {
  console.error('Supabase Error:', error);
  // エラーハンドリング
}
```

---

## 🔒 セキュリティ対策

### 1. XSS対策
- Reactによる自動エスケープ
- `dangerouslySetInnerHTML` の使用禁止

### 2. CSRF対策
- Next.jsの標準CSRF保護
- SameSite Cookie属性

### 3. SQL Injection対策
- Supabase ORMによるパラメータバインディング
- 生SQLの使用禁止

### 4. 認証・認可
- 全APIでセッション確認
- RLSによるデータアクセス制御
- ユーザー種別による機能制限

### 5. 機密情報管理
- 環境変数で管理
- `.env.local` をGit管理外
- サーバーサイドのみでアクセス

---

## 📚 コーディング規約

### TypeScript
```typescript
// インターフェース名は先頭にIを付けない
interface User {
  id: string;
  email: string;
}

// 型エイリアスも活用
type UserType = 'company' | 'sales_agency' | 'admin';

// nullableは明示的に
name: string | null;

// 関数の戻り値は明示
function getUser(): User | null {
  // ...
}
```

### コンポーネント
```typescript
// 関数コンポーネント
export default function ComponentName() {
  // ...
}

// propsは型定義
interface Props {
  title: string;
  onClick: () => void;
}

export function Component({ title, onClick }: Props) {
  // ...
}
```

### ファイル命名
- コンポーネント: PascalCase（`AgencyCard.tsx`）
- ユーティリティ: camelCase（`utils.ts`）
- 定数: UPPER_SNAKE_CASE（`CONSTANTS.ts`）

---

## 🔄 Git運用

### ブランチ戦略

```
main          ← 本番環境
 └── develop  ← 開発環境
      ├── feature/auth
      ├── feature/agencies
      ├── feature/matching
      ├── feature/messages
      └── feature/admin
```

### コミットメッセージ

```
feat: 新機能追加
fix: バグ修正
docs: ドキュメント更新
style: コードスタイル変更
refactor: リファクタリング
test: テスト追加
chore: その他の変更
```

例:
```
feat: 営業代行検索機能を実装
fix: メッセージ送信時のエラーを修正
docs: API設計書を更新
```

---

## 📦 依存パッケージ一覧

```json
{
  "dependencies": {
    "next": "^15.0.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "@supabase/supabase-js": "^2.39.0",
    "@supabase/auth-helpers-nextjs": "^0.9.0",
    "react-hook-form": "^7.49.0",
    "zod": "^3.22.0",
    "@hookform/resolvers": "^3.3.0",
    "date-fns": "^3.0.0",
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.2.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0",
    "eslint": "^8.56.0",
    "eslint-config-next": "^15.0.0",
    "prettier": "^3.1.0"
  }
}
```

---

## 🔍 トラブルシューティング

### よくある問題と解決策

#### 1. Supabase接続エラー
```
Error: Invalid Supabase URL or API key
```
**解決策:** `.env.local` の設定を確認

#### 2. Realtime接続失敗
```
Error: Realtime channel subscription failed
```
**解決策:** Supabase Dashboard → Database → Replicationでテーブルを有効化

#### 3. RLSエラー
```
Error: new row violates row-level security policy
```
**解決策:** RLSポリシーを確認、またはauth.uid()が正しく設定されているか確認

#### 4. CORS エラー
```
Error: CORS policy blocked
```
**解決策:** Supabase Dashboard → API Settings → CORSで許可するオリジンを追加

---

## 📝 開発メモ

### 注意事項
- Server ComponentとClient Componentの使い分けに注意
- Supabaseクライアントは適切な場所で初期化（server vs client）
- Realtimeチャンネルは必ずクリーンアップ（useEffectのreturn）

### ベストプラクティス
- TypeScript型定義を活用
- エラーハンドリングを徹底
- ローディング状態を適切に表示
- アクセシビリティを考慮

---

**作成日**: 2025-12-04  
**バージョン**: 1.0  
**最終更新**: 2025-12-04

